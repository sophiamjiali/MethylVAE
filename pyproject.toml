[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "MethylVAE"
version = "0.1.0"

[tool.setuptools]
packages = ["MethylVAE"]
package-dir = {"" = "src"}
